# Claims Adjudication AI — Streamlit App


## Setup

```bash
pip install -r requirements.txt
streamlit run app.py
Supply your OPENAI_API_KEY

Note: 
The actual code is in pipeline.py
Streamlit and SQLLite database code were generated with Claude to wrap around the pipeline to visually display the
pipeline stages
```

